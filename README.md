# Content

This repository holds all the learning content as Markdown files.

The platform scans this folder automatically, so adding any `.md` file:

- makes it appear in the sidebar automatically, and
- gives it a URL matching its folder path.

Example:

```text
programming/javascript/basics/loops.md
```

becomes:

```text
/programming/javascript/basics/loops
```
